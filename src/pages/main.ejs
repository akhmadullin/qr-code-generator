<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QR code qenerator</title>
    <style>
        body {
            margin: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen',
                'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue',
                sans-serif;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            font-size: 16px;
        }
        .container {
            padding: 20px;
        }
        h1, p {
            margin: 0;
            padding: 0;
        }

        .centered-text {
            text-align: center;
        }

        .form {
            display: grid;
            grid-gap: 20px;
            max-width: 350px;
            margin: 0 auto;

        }

        @media (max-widht: 414px) {
            .form {
                max-width: 100%;
            }
        }

        .btn {
            border: none;
            background: #000000;
            color: #ffffff;
            padding: 10px 15px;
            cursor: pointer;
            font-weight: bold;
            font-size: 16px;
            height: 40px;
            box-sizing: border-box;
            border-radius: 6px;
        }

        .btn:hover {
            opacity: 0.7;
        }

        .text-field {
            height: 40px;
            line-height: 40px;
            font-size: 16px;
            box-sizing: border-box;
            width: 100%;
            border-radius: 6px;
            padding: 5px;
            border: 2px solid black;
        }

        .mb-10 {
            margin-bottom: 10px;
        }

        .mb-20 {
            margin-bottom: 10px;
        }

        .mb-40 {
            margin-bottom: 40px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="centered-text mb-20">QR code generator</h1>
        <p class="centered-text mb-40">Our service will generate QR code to your's URL</p>
        <form class="form" action="/qrcode" method="POST">
            <label>
                <div class="mb-10">Name</div>
                <input name="name" class="text-field" placeholder="Type name related with URL" type="text">
            </label>
            <label>
                <div class="mb-10">URL</div>
                <input name="url" class="text-field" placeholder="Type URL" type="text" required>
            </label>
            <button type="submit" class="btn">Generate</button>
        </form>
    </div>
</body>
</html>